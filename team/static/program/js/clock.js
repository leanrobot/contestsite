// Generated by CoffeeScript 1.7.1
(function() {
  $(document).ready(function() {
    var dateString, end;
    dateString = $('#timestamp').text();
    end = Date.parse(dateString);
    return window.setInterval(function() {
      var clockText, delta, format, hours, minutes, seconds;
      delta = end - Date.now();
      format = function(num) {
        if (num / 10 < 1) {
          return "0" + num;
        }
        return num;
      };
      seconds = Math.floor((delta / 1000) % 60);
      minutes = Math.floor((delta / (1000 * 60)) % 60);
      hours = Math.floor(delta / (1000 * 60 * 60));
      clockText = "" + (format(hours)) + ":" + (format(minutes)) + ":" + (format(seconds));
      return $('#time').text(clockText);
    }, 500);
  });

}).call(this);
